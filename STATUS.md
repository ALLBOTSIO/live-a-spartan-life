# Live a Spartan Life — Status

## Site Information

**Client:** JROC (Jason Craig)  
**Agency:** AIVH  
**Status:** Active development  
**Production URL:** https://www.liveaspartanlife.com  
**Repository:** https://github.com/ALLBOTSIO/live-a-spartan-life

## Current State (3 Sep 2026)

### Live Site
- **Production deployment:** Active on Vercel
- **Domain:** liveaspartanlife.com (www and apex)
- **Framework:** Next.js 16 (App Router)
- **Content:** MDX-based Field Notes journal with five-pillar system

### Recent Fixes (3 Sep 2026)

#### Critical Crawl Issues Resolved
1. **robots.txt localhost bug** — Production was shipping `Sitemap: http://localhost:3000/sitemap.xml`. Fixed by inferring site URL from Vercel's `VERCEL_URL` environment variable when `NEXT_PUBLIC_SITE_URL` is not explicitly set.

2. **sitemap.xml 500 errors** — Added defensive error handling to prevent sitemap generation failures from crashing the route. Articles that fail to load are logged but don't block the static routes from appearing.

3. **Content-Type headers** — Next.js metadata routes automatically set correct headers (`text/plain` for robots.txt, `application/xml` for sitemap.xml).

### Environment Configuration

The site uses `NEXT_PUBLIC_SITE_URL` for canonical URLs, Open Graph tags, sitemap generation, and Stripe redirects. When this variable is not set:
- **Production (Vercel):** Automatically uses `https://{VERCEL_URL}`
- **Development:** Falls back to `http://localhost:3000`

For custom domains, set `NEXT_PUBLIC_SITE_URL=https://www.liveaspartanlife.com` in Vercel environment variables to override the automatic inference.

### Integration Status

- ✅ Supabase (Auth + Postgres)
- ✅ Stripe (Brotherhood subscription at $24/mo)
- ✅ Resend (Email + newsletter)
- ✅ PostHog (Analytics)
- ⚠️ Hero/founder images — Some placeholders may remain ("Image reserved")

## Deployment

- **Platform:** Vercel
- **Branch strategy:** `main` is production
- **CI/CD:** Automatic deployments on push to `main`
- **Preview deployments:** Every PR gets a unique preview URL

## Next Steps

1. Verify robots.txt and sitemap.xml are serving correct production URLs post-deployment
2. Submit updated sitemap to Google Search Console
3. Replace any remaining placeholder images with final assets
4. Monitor crawl status in Google Search Console

## Maintenance Notes

- **robots.txt:** Generated by `app/robots.ts` — do not create a static `public/robots.txt`
- **sitemap.xml:** Generated by `app/sitemap.ts` — dynamically includes all Field Notes articles
- **Content:** Field Notes articles live in `content/field-notes/` as MDX files
- **Five pillars:** Train, Fuel, Mind, Provide, Brotherhood (final brand copy)

## Contact

For technical issues or deployment questions, contact the AIVH development team.  
For content, brand, or strategy questions, contact JROC directly.
